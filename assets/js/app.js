import {debounce} from "./debounce";
import {throttle} from "./throttle";
import {inputSync} from "./input-sync";

window.debounce = debounce;
window.throttle = throttle;
window.inputSync = inputSync;
