{% extends 'layouts/base.html.twig' %}
{% form_theme form 'common/forms/bundle.html.twig' %}

{% block body %}
    <div class="container container-y">
        {% for type, alerts in app.flashes %}
            {% for message in alerts %}
                <div class="alert alert-{{ type }} alert-dismissible fade show" role="alert">
                    {{ message|trans|raw }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        {% endfor %}

        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h2 class="mb-0">{{ 'user.application.title'|trans({application_id: application.id, posting_title: application.posting.title}) }}</h2>
            </div>
            <div class="card-body">
                <div class="card mb-3">
                    <div class="card-header">
                        {{ 'user.application.details.header'|trans }}
                    </div>
                    <div class="card-body">
                        {% include 'components/posting/application_details.html.twig' %}
                    </div>
                </div>

                {% set answersBody %}
                    {{ form_start(form) }}
                    <dl class="row mb-0">
                        {#                        TODO: Export into a common? #}
                        {{ form_row(form.schedule) }}
                        {% set answers = form.children|filter(field => field.vars.name starts with 'answer_') %}
                        {% for field in answers %}
                            <div class="mb-3">
                                {{ form_row(field) }}
                            </div>
                        {% endfor %}
                    </dl>
                    <div>
                        <button type="submit" class="btn btn-primary">
                            <i class="fa-solid fa-user"></i>
                            {{ 'common.save'|trans }}
                        </button>
                    </div>
                    {{ form_end(form) }}
                {% endset %}
                {% embed 'common/accordion.html.twig' %}
                    {% embed 'common/accordion-item.html.twig' %}
                        {% block buttonText %}
                            <b>{{ 'user.application.questionnaire.header'|trans }}</b>
                        {% endblock %}
                        {% block body %}
                            {{ answersBody|raw }}
                        {% endblock %}
                    {% endembed %}
                {% endembed %}
                <div class="mt-3">
                    <a href="{{ path('app_admin_posting_application_delete', {applicationId: application.id, id: application.posting.id}) }}"
                       class="btn btn-danger">
                        <i class="fa-solid fa-trash"></i>
                        {{ 'common.delete'|trans }}
                    </a>
                </div>
            </div>
            <div class="card-footer">
                {{ 'user.application.help'|trans({'email': application.posting.assignedTo.email })|raw }}
            </div>
        </div>
    </div>
{% endblock %}

